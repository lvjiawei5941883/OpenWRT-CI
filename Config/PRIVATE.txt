# SPDX-License-Identifier: MIT
# 老板的私有内核配置 —— 额外要编译进固件的 CONFIG_ 项放这里
# 这个文件不会被 Sync-Upstream 覆盖，永远安全
# Settings.sh 会自动 >> 到 .config 末尾

# 示例格式（去掉前面的 # 取消注释）：
# CONFIG_PACKAGE_luci-app-xxx=y
（内容由AI生成，仅供参考）
